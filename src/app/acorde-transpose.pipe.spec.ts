import { AcordeTransposePipe } from './acorde-transpose.pipe';

describe('AcordeTransposePipe', () => {
  it('create an instance', () => {
    const pipe = new AcordeTransposePipe();
    expect(pipe).toBeTruthy();
  });
});
