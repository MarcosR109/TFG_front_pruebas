import { AcordeTransformPipe } from './acorde-transform.pipe';

describe('AcordeTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new AcordeTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
